# Runtime dependencies
# Allow newer PySide6 compatible with newer Python (e.g. Python 3.13+)
PySide6>=6.10.0
requests==2.31.0

# Bundling / build tools
# PyInstaller used to create executables
PyInstaller==6.16.0
# Pillow used to generate .ico from PNG during build
Pillow>=9.0.0

# Optional runtime helpers that may be imported by hooks
simplejson>=3.18.0
importlib-resources>=5.12.0

# Development/test dependencies
pytest==8.3.3
pytest-qt==4.4.0
